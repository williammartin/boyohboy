commands:
  entrypoint:
    format: --ticker {{ticker}}
    help: Fetches the latest trade price for a stock ticker
    arguments:
      ticker:
        help: Stock ticker symbol
        type: string
        required: true
